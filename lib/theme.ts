import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#10812C",
    },
    secondary: {
      main: "#A63D93",
    },
    background: {
      paper: "#f7f4f0",
    },
  },
});

export default theme;
